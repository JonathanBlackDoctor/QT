{
  "name": "qt-daily-commentary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build-site.mjs",
    "test": "node --test scripts/*.test.mjs"
  }
}
